class UsersController < ApplicationController
	# def new
	# 	@user = User.new
	# end
	# def create
	# 	@user = User.new(user_params)
	# 	if @user.save
	# 		session[:user_id] = @user.id
	# 		redirect_to root_url
	# 	else
	# 		render 'new'
	# end
	# def user_params
	# 	params.reqire(:user).permit(:username, :password, :email_address, :role_id)
	# end
end
